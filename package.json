{
  "name": "vibe-coding-car-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Curriculum monorepo: Web remote controller + ESP32-C3 vehicle firmware + shared protocol",
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npx turbo run dev",
    "build": "npx turbo run build",
    "test": "npx turbo run test",
    "lint": "npx turbo run lint",
    "ota": "node scripts/ota_upload.mjs"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.3"
  }
}
